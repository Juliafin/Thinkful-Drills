var pageViewCounts = {
  homePage: 399,
  aboutPage: 400,
  termsOfService: 22
};

console.log(Object.keys(pageViewCounts));

function adding( element, ) {
  console.log(_)
}

var instruments = {}
Object.keys(pageViewCounts).forEach(function(key){
  console.log(
    'the `' + key + '` page has ' + pageViewCounts[key] +
    ' views.'
  );
})

var someAry = [1, 2, 3];
var newAry = [];
// null, 0, ""
forEach(function(item, index, someAry) {
  if(index === 0) {
    newArray.push(undefined);
    newArray.push(item);
  } else {
    newArray.push(item);
  }
});

for (var i=0; i<ary.length; i++) {
  var item = ary[i];
  var index = i;
  var array = ary;

  function yourFunction(item, index, array) {
    // whatever you decided
  }

  yourFunction(item, index, array);
}
// ary = [2, 3, 4]

[1, 2, 3].map(function(item, index, ary) {
  return item + 1;
});
